"""Node 3: LLM-based exam generation — Tunisian 6th-grade multi-step difficulty."""

import os
import random
import re as _re
from dotenv import load_dotenv
from graph.nodes.llm_utils import get_llm, invoke_with_retry
from langchain_core.prompts import ChatPromptTemplate

load_dotenv()

# ══════════════════════════════════════════════════════════════════════════════
# Scenario pools — randomly picked each run to ensure variety
# ══════════════════════════════════════════════════════════════════════════════

_SCENARIO_THEMES = [
    "مشروع بناء مدرسة جديدة في الريف",
    "تحضير حفل نهاية السنة الدراسية",
    "رحلة مدرسية إلى مدينة سوسة",
    "مشروع فلاحي لغراسة أشجار الزيتون",
    "بناء خزّان مائي لفائدة القرية",
    "إعداد بازار خيري لجمع تبرّعات",
    "تهيئة ملعب رياضي للمدرسة",
    "شراء تجهيزات لمكتبة المدرسة",
    "إنشاء حديقة عموميّة في الحيّ",
    "تجارة تمور ومنتجات فلاحية في السوق",
    "إصلاح منزل العائلة وتوسيعه",
    "رحلة عائلية إلى جزيرة جربة",
    "تنظيم مسابقة رياضيّة بين المدارس",
    "مشروع تربية نحل وإنتاج عسل",
    "تحضير معرض علمي في المدرسة",
    "شراء أرض وبناء مسكن",
    "تجهيز ورشة خياطة للأمّهات",
    "إنجاز مشروع سياحي صغير",
    "إصلاح وتبليط قاعة متعدّدة الاختصاصات",
    "تنظيم قافلة صحّية في القرى",
]

_PROTAGONIST_NAMES = [
    "أحمد", "سعيد", "سامي", "ياسين", "مريم", "خالد",
    "نبيل", "فاطمة", "عمر", "صالح", "آمنة", "محمّد",
    "ليلى", "كريم", "سناء", "إبراهيم", "هند", "رشيد",
]

_GEOMETRY_SHAPES = [
    "مستطيل", "مربّع", "مثلّث", "شبه منحرف قائم الزاوية",
    "متوازي أضلاع", "معيّن", "مثلّث متساوي الساقين",
]

# ── System prompt: very specific about difficulty, structure, and realism ─────

SYSTEM_PROMPT = """أنت خبير في إعداد فروض مراقبة الرياضيات للسنة السادسة ابتدائي — النظام التونسي الرسمي.
يجب أن يكون الفرض المنتج بمستوى صعوبة حقيقي يتطلّب تفكيرا متعدد المراحل.

══════════════════ قاعدة ذهبية: الإبداع والتفرّد ══════════════════

⚠️ يجب أن يكون كلّ فرض تُنشئه فريدا تماما ومختلفا عن أيّ فرض سابق:
• اختر وضعية اجتماعية/اقتصادية/تربوية جديدة لم تُستعمل من قبل
• استعمل أعدادا وأرقاما مختلفة كلّيا في كلّ مرّة
• غيّر ترتيب أنواع الأسئلة (لا تبدأ دائما بالحساب)
• نوّع في الأشكال الهندسية المطلوبة
• لا تنسخ ولا تحاكي الأمثلة — استعملها فقط لفهم المستوى والشكل

══════════════════ الهيكل الإلزامي ══════════════════

الفرض يحتوي على وضعية واحدة كبيرة مأخوذة من الحياة اليومية الواقعية التونسية.
تتفرع هذه الوضعية إلى 3 تمارين، كل تمرين يحتوي:
• سند (فقرة سردية متصلة بالوضعية الكبرى فيها معطيات عددية كافية)
• عدد متنوّع من التعليمات (2 إلى 4 تعليمات حسب التمرين)

القواعد الحسابية والبنيوية:
1. المجموع الكلي = 20 نقطة بالضبط
2. كل تعليمة تُحدّد نقاطها مثل: (2.5 ن)
3. كل تمرين يُحدّد مجموع نقاطه مثل: تمرين 1 (7 ن)
4. مجموع نقاط التعليمات = مجموع نقاط التمرين (بالضبط)
5. لا تقل أي تعليمة عن 1.5 ن ولا تزيد عن 5 ن
6. عدد التعليمات الكلي: بين 7 و 10 تعليمات
7. نوّع في عدد التعليمات: مثلا (3 تعليمات، ثم 2 تعليمات، ثم 4 تعليمات)

══════════════════ مستوى الصعوبة ══════════════════

❌ ممنوع: أسئلة بعملية واحدة (جمع / طرح / ضرب فقط)
✅ مطلوب: كل تعليمة تتطلب خطوتين (2) على الأقل من الحل

أنواع الأسئلة التي يجب تضمينها (على الأقل 4 من الأنماط التالية):
- حساب بالأعداد الكبيرة أو العشرية يمر بمرحلتين (مثلا: أحسب ثمن X ثم أستخرج الباقي)
- "أثبت أن…" (أعطِ النتيجة في السند واطلب إثباتها بالعمليات)
- حساب المدة الزمنية (ساعات + دقائق، تحويل بين وحدات)
- حساب المسافة الحقيقية من خريطة بسلم رسم
- حساب محيط أو مساحة مع معطيات ناقصة تستوجب استنتاجا
- كسور عادية: جمع أو طرح أو مقارنة مع توحيد مقامات
- تناسبية: إكمال جدول أو قاعدة ثلاثة
- إنشاء هندسي: رسم مثلث أو شبه منحرف بمعطيات (زوايا + أطوال)
- تحويل وحدات: كغ ↔ غ، كم ↔ م، ل ↔ مل، د ↔ مي
- نسب مائوية: حساب نسبة أو تطبيق نسبة على مبلغ

══════════════════ الأرقام ══════════════════

• استعمل أعدادا واقعية كبيرة (مئات الآلاف للأموال، أوزان بالأطنان، مسافات حقيقية)
• لا تستعمل أرقاما مستديرة سهلة (تجنب 100، 200، 500)
• استعمل أعدادا عشرية (مثل 17.500 د، 3.75 كم، 2.400 د)
• الكسور يجب أن تكون غير بسيطة (تحتاج توحيد مقامات)

══════════════════ معطيات كاملة ══════════════════

⚠️ مهمّ جدا: كل سند يجب أن يحتوي على جميع الأرقام والمعطيات اللازمة لحلّ تعليماته.
لا تترك أي معطى ناقصا. إذا طلبت حساب ثمن، أعطِ ثمن الوحدة والكمية.
إذا طلبت حساب مساحة، أعطِ جميع الأبعاد اللازمة.
إذا طلبت "أثبت أن"، تأكّد أنّ النتيجة المطلوب إثباتها صحيحة فعلا حسابيا.

══════════════════ الشكل المطلوب بالترتيب الصحيح ══════════════════

⚠️ مهم جدا: اتبع هذا الترتيب بالضبط ولا تعكس النص!

1. عنوان التمرين ونقاطه
2. السند: فقرة كاملة متماسكة من 2-4 جمل
3. التعليمات: واحدة تلو الأخرى

مثال صحيح:

تمرين 1 (7 ن)
السند 1: قرر أحمد شراء أرض لبناء منزل. تبلغ مساحة الأرض 475 م² وثمن المتر المربع الواحد 125.500 د. دفع أحمد مبلغا أوليا قدره 25000 د والباقي على 12 قسطا متساويا.
التعليمة 1 (2.5 ن): احسب الثمن الجملي للأرض.
التعليمة 2 (2.5 ن): احسب المبلغ المتبقي بعد الدفعة الأولية.
التعليمة 3 (2 ن): احسب قيمة القسط الشهري الواحد.

تمرين 2 (6 ن)
السند 2: لبناء المنزل، استعان أحمد بـ 8 عمال. بدأ العمل يوم الاثنين على الساعة 7 و 30 دقيقة صباحا وانتهى يوم الجمعة على الساعة 5 مساء. يتقاضى كل عامل 28.750 د عن اليوم الواحد.
التعليمة 1 (2 ن): احسب عدد أيام العمل.
التعليمة 2 (2 ن): احسب الأجر الجملي لعامل واحد.
التعليمة 3 (2 ن): احسب الأجر الجملي لجميع العمال.

تمرين 3 (7 ن)
السند 3: صمم أحمد غرفة على شكل مستطيل طولها 6.5 م وعرضها 4 م. يريد رسم مخططها باستعمال السلم 1 صم = 1 م.
التعليمة 1 (2 ن): احسب مساحة الغرفة.
التعليمة 2 (2 ن): احسب محيط الغرفة.
التعليمة 3 (3 ن): ارسم مخطط الغرفة باعتماد السلم المذكور.

⚠️ لا تكتب السند على عدة أسطر متفرقة! اكتبه كفقرة واحدة متماسكة بعد "السند 1:" مباشرة.

══════════════════ مثال على سؤال متعدد المراحل ══════════════════

بدلا من: "أحسب 250 + 120" ← هذا سهل جدا ❌
اكتب: "علمت أن ثمن الكيلوغرام الواحد من التمور 2.400 د. أثبت أن المبلغ الجملي لبيع المحصول يساوي 92400 د" ← هذا يتطلب: معرفة الكمية الكلية + الضرب + التحقق ✅

اكتب بالعربية الفصحى فقط. لا تضف عناوين markdown (لا # ولا ##).
"""

HUMAN_TEMPLATE = """الثلاثي: {trimester}

══ موضوع الفرض المطلوب ══
الوضعية الكبرى: {scenario_theme}
الشخصية الرئيسية: {protagonist}
الشكل الهندسي المطلوب: {geometry_shape}

المحاور المطلوبة لهذا الثلاثي:
{curriculum_text}

فيما يلي {num_examples} أمثلة من فروض سابقة حقيقية تونسية كمرجع للمستوى والتعقيد فقط (لا تنسخ منها!):

{reference_texts}

---

أنشئ الآن فرض مراقبة جديدا وكاملا للثلاثي {trimester}.

⚠️ قواعد التنسيق الإلزامية:

1. الترتيب الصحيح لكل تمرين:
   - السطر الأول: تمرين X (Y ن)
   - السطر الثاني: السند X: [فقرة كاملة من 2-4 جمل]
   - الأسطر التالية: التعليمات واحدة تلو الأخرى

2. السند يجب أن يكون:
   - فقرة واحدة متماسكة (ليس جملا منفصلة)
   - مكتوبة مباشرة بعد "السند X:" على نفس السطر
   - تحتوي على كل المعطيات الرقمية اللازمة
   - لا تستخدم أسماء أماكن بالإنجليزية (استخدم أسماء تونسية عربية)

3. التعليمات:
   - كل تعليمة على سطر منفصل
   - تبدأ بـ "التعليمة X (Y ن):"
   - واضحة ومختصرة

التزم بـ:
• الوضعية المحدّدة أعلاه ({scenario_theme}) مع الشخصية ({protagonist})
• 3 تمارين بها 7 إلى 10 تعليمات إجمالا
• نوّع في عدد التعليمات: مثلا تمرين 1 (3 تعليمات)، تمرين 2 (2 تعليمات)، تمرين 3 (4 تعليمات)
• كل تعليمة تحتاج خطوتين (2) على الأقل في الحل
• على الأقل تعليمة واحدة من نوع "أثبت أنّ"
• تعليمة هندسية واحدة على الأقل (رسم {geometry_shape})
• المجموع = 20 نقطة بالضبط
• أرقام مختلفة تماما عن الأمثلة — لا تنسخ أيّ رقم
• استخدم أسماء مدن تونسية (تونس، سوسة، صفاقس، القيروان، المنستير، قابس...)
• تحقّق أنّ أيّ سؤال "أثبت أنّ" نتيجته صحيحة حسابيا
• لا تضف markdown (# أو **)
• ابدأ مباشرة بـ "تمرين 1" بدون مقدمة
"""


def _format_curriculum(curriculum: dict) -> str:
    """Format the curriculum dict into readable Arabic text."""
    lines = []
    for topic, skills in curriculum.items():
        skills_text = "، ".join(skills)
        lines.append(f"- {topic}: {skills_text}")
    return "\n".join(lines)


def _format_references(reference_exams: list[dict]) -> str:
    """Format reference exam texts for injection into the prompt."""
    parts = []
    for i, exam in enumerate(reference_exams, 1):
        text = exam.get("full_text", "")
        if len(text) > 3500:
            text = text[:3500] + "\n... (مختصر)"
        parts.append(f"=== مرجع {i} ===\n{text}")
    return "\n\n".join(parts)


def generator_node(state: dict) -> dict:
    """LangGraph node: generate a challenging, unique multi-step exam."""
    trimester = state["trimester"]
    curriculum = state.get("curriculum", {})
    reference_exams = state.get("reference_exams", [])

    curriculum_text = _format_curriculum(curriculum)
    reference_texts = _format_references(reference_exams)

    # ── Random seeds for variety ──
    scenario_theme = random.choice(_SCENARIO_THEMES)
    protagonist = random.choice(_PROTAGONIST_NAMES)
    geometry_shape = random.choice(_GEOMETRY_SHAPES)

    prompt = ChatPromptTemplate.from_messages([
        ("system", SYSTEM_PROMPT),
        ("human", HUMAN_TEMPLATE),
    ])

    llm = get_llm(temperature=0.8, max_tokens=4096)

    chain = prompt | llm

    response = invoke_with_retry(chain, {
        "trimester": trimester,
        "curriculum_text": curriculum_text,
        "num_examples": len(reference_exams),
        "reference_texts": reference_texts,
        "scenario_theme": scenario_theme,
        "protagonist": protagonist,
        "geometry_shape": geometry_shape,
    })

    exam_text = _postprocess(response.content.strip())

    return {"exam_text": exam_text}


# ── Post-processing ──────────────────────────────────────────────────────────

def _postprocess(text: str) -> str:
    """Clean up LLM output to match expected format exactly."""
    # Remove markdown headers (# / ## / ### / **)
    text = _re.sub(r'^#{1,4}\s*', '', text, flags=_re.MULTILINE)
    text = text.replace('**', '')

    # Remove only introductory fluff (like "إليك" "فيما يلي" etc) but keep scenario context
    # Only remove if there's clearly non-exercise content at the start
    intro_patterns = [
        r'^.*?(?=تمرين\s*\d+)',  # Everything before first exercise
    ]
    
    # Only remove if it looks like LLM preamble (contains certain phrases)
    first_exercise = _re.search(r'تمرين\s*\d+', text)
    if first_exercise:
        before_exercise = text[:first_exercise.start()].strip()
        # Check if it's just LLM fluff (typical phrases)
        fluff_patterns = [r'إليك', r'فيما يلي', r'الفرض التالي', r'الفرض المقترح', 
                         r'هذا هو', r'بناء على', r'كما طلبت']
        is_fluff = any(_re.search(p, before_exercise, _re.IGNORECASE) for p in fluff_patterns)
        
        # Only remove if it's clearly LLM-generated preamble (short and contains fluff)
        if is_fluff and len(before_exercise) < 200:
            text = text[first_exercise.start():]

    # Normalize "نقاط" / "نقطة" → "ن"
    text = _re.sub(r'\((\d+(?:[.,]\d+)?)\s*نقاط?\)', r'(\1 ن)', text)
    text = _re.sub(r'\((\d+(?:[.,]\d+)?)\s*نقطة?\)', r'(\1 ن)', text)

    # Normalize "سند:" → "السند:"
    text = _re.sub(r'^سند\s*:', 'السند:', text, flags=_re.MULTILINE)

    # Ensure "التعليمة" not "تعليمة"
    text = _re.sub(r'^تعليمة\s+(\d+)', r'التعليمة \1', text, flags=_re.MULTILINE)

    return text.strip()
